export class IconItems {
  title:string;
  iconName:string;
  routingLink:string;

  constructor(title: string, iconName: string, routingLink: string) {
    this.title = title;
    this.iconName = iconName;
    this.routingLink = routingLink;
  }
}
