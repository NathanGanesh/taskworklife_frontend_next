import { CustomDateAdapter } from './custom-date-adapter';

describe('CostumDateAdapter', () => {
  it('should create an instance', () => {
    expect(new CustomDateAdapter()).toBeTruthy();
  });
});
